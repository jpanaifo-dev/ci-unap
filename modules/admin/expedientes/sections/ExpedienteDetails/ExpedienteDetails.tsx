import { IEnrollment } from '@/types'

interface IProps {
  expedienteData: IEnrollment
}

export const ExpedienteDetails = (props: IProps) => {
  const { expedienteData } = props
  return (
    <main>
      <section>
        <h1></h1>
      </section>
    </main>
  )
}
